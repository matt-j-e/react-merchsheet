import Styled from "styled-components";

export const Logo = Styled.h1`
  font-family: 'Monoton', cursive;
  color: var(--midblue);
  font-size: 5.5rem;
  text-align: center;
  padding: 0;
  margin: 0;
`;